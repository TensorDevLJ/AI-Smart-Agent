# Smart Agent - Frontend (React)

## Run locally
cd frontend
npm install
npm start

The React app connects to backend at http://localhost:8000 by default.
You can change the base URL in src/config.js
